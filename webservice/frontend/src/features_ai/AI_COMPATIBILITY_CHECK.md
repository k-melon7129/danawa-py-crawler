# 🤖 AI 호환성 검사 시스템 (17개 필수 체크)

## 📋 개요

PC 부품 조립 시 **절대 필수적인 17가지 호환성**을 자동으로 검증하는 시스템입니다.  
사용자가 선택한 필터 조건을 기반으로 AI가 실시간으로 호환성을 분석하고, 문제가 있을 경우 **즉시 경고 및 해결 방안**을 제시합니다.

---

## ✅ 17가지 필수 호환성 체크 리스트

### 🔥 1. CPU 소켓 ↔ 쿨러 소켓
**검증 내용**: CPU 소켓과 쿨러 소켓이 일치하는지 확인

**예시**:
- ✅ **정상**: CPU (LGA1700) + 쿨러 소켓 (LGA1700)
- ❌ **오류**: CPU (LGA1700) + 쿨러 소켓 (AM5)

**해결 방안**: 쿨러 호환 소켓에서 CPU 소켓과 동일한 것을 선택

---

### 🔥 2. CPU TDP ↔ 쿨러 TDP (냉각 능력)
**검증 내용**: 쿨러가 CPU 발열을 충분히 식힐 수 있는지 확인

**예시**:
- ✅ **정상**: CPU TDP 125W + 쿨러 TDP 150W
- ❌ **오류**: CPU TDP 150W + 쿨러 TDP 100W (과열 위험)

**해결 방안**: 쿨러 TDP를 CPU TDP 이상으로 설정

---

### 🔥 3. CPU 소켓 ↔ 메인보드 칩셋
**검증 내용**: CPU와 메인보드 칩셋이 호환되는지 확인

**예시**:
- ✅ **정상**: Intel 14세대 CPU + Z790 칩셋
- ❌ **오류**: Intel 14세대 CPU + X670E 칩셋 (AMD 전용)

**해결 방안**: CPU 세대에 맞는 칩셋 선택
- Intel 14세대: Z790, B760
- Intel 13세대: Z790, Z690, B760, B660
- Ryzen 7000: X670E, B650
- Ryzen 5000: X570, B550, B450 (BIOS 업데이트 필요)

---

### 🔥 4. CPU 세대 ↔ 메인보드 BIOS 지원
**검증 내용**: 메인보드 BIOS가 CPU 세대를 지원하는지 확인

**특별 주의**:
- ⚠️ **Ryzen 5000 + B450**: BIOS 업데이트 필수
- ⚠️ **Intel 13세대 + B660**: BIOS 업데이트 권장

**예시**:
- ✅ **정상**: Ryzen 7000 + X670E (네이티브 지원)
- ⚠️ **경고**: Ryzen 5000 + B450 (BIOS 업데이트 필요)

---

### 🔥 5. RAM 타입 ↔ 메인보드
**검증 내용**: RAM 세대(DDR4/DDR5)가 메인보드와 호환되는지 확인

**중요**: DDR4와 DDR5는 **물리적으로 완전히 다른 규격**이며 절대 호환되지 않음

**예시**:
- ✅ **정상**: RAM DDR5 + 메인보드 DDR5 지원
- ❌ **오류**: RAM DDR5 + 메인보드 DDR4만 지원

---

### 🔥 6. RAM 클럭 ↔ CPU/메인보드 지원 (XMP/EXPO)
**검증 내용**: RAM 속도가 메인보드 최대 지원 속도 이내인지 확인

**예시**:
- ✅ **정상**: RAM 6000MHz + 메인보드 최대 7200MHz
- ⚠️ **경고**: RAM 7600MHz + 메인보드 최대 7200MHz (속도 제한됨)

**XMP/EXPO**:
- **XMP**: Intel 플랫폼 RAM 오버클럭
- **EXPO**: AMD 플랫폼 RAM 오버클럭

---

### 🔥 7. RAM 최대 용량 ↔ 메인보드
**검증 내용**: RAM 용량이 메인보드 최대 용량 이내인지 확인

**예시**:
- ✅ **정상**: RAM 64GB + 메인보드 최대 128GB
- ❌ **오류**: RAM 128GB + 메인보드 최대 64GB

**참고**: 
- 일반 메인보드: 64GB~128GB
- 고급 메인보드: 128GB~192GB

---

### 🔥 8. 쿨러 높이 ↔ 케이스 내부 공간
**검증 내용**: 공랭 쿨러 높이가 케이스 최대 높이 이내인지 확인

**예시**:
- ✅ **정상**: 쿨러 160mm + 케이스 최대 170mm
- ❌ **오류**: 쿨러 170mm + 케이스 최대 160mm (물리적 장착 불가)

**참고**: 
- 미들타워: 보통 160~180mm
- 풀타워: 보통 180~200mm
- 미니케이스: 보통 100~140mm

---

### 🔥 9. 메인보드 폼팩터 ↔ 케이스 지원
**검증 내용**: 메인보드 크기가 케이스에 장착 가능한지 확인

**폼팩터 크기 순서**: E-ATX > ATX > M-ATX > Mini-ITX

**예시**:
- ✅ **정상**: ATX 메인보드 + 미들타워 (ATX 지원)
- ❌ **오류**: ATX 메인보드 + SFF 케이스 (Mini-ITX만 지원)

---

### 🔥 10. M.2 슬롯 수 ↔ NVMe SSD 개수
**검증 내용**: 메인보드 M.2 슬롯이 NVMe SSD 장착에 충분한지 확인

**예시**:
- ✅ **정상**: NVMe SSD 1개 선택 + 메인보드 M.2 슬롯 2개
- ❌ **오류**: NVMe SSD 1개 선택 + 메인보드 M.2 슬롯 0개

**참고**: 대부분 메인보드는 2~4개 M.2 슬롯 제공

---

### 🔥 11. SATA 포트 수 ↔ SATA 장치 개수
**검증 내용**: 메인보드 SATA 포트가 SATA SSD + HDD 장착에 충분한지 확인

**예시**:
- ✅ **정상**: SATA SSD 1개 + HDD 1개 = 2포트 필요, 메인보드 4포트
- ❌ **오류**: SATA SSD 1개 + HDD 2개 = 3포트 필요, 메인보드 2포트

**참고**: 
- HDD는 항상 SATA 사용
- SATA SSD는 SATA 포트 또는 M.2(SATA) 사용 가능

---

### 🔥 12. M.2 장착 ↔ SATA 포트 충돌 (PCIe 레인 공유)
**검증 내용**: M.2 SSD 장착 시 일부 SATA 포트가 비활성화되는지 확인

**예시**:
- ⚠️ **경고**: M.2 슬롯 1개 사용 시 SATA 포트 2개 비활성화
- 💡 **제안**: 메인보드 매뉴얼에서 M.2/SATA 레인 공유 확인

**이유**: 일부 메인보드는 M.2와 SATA가 PCIe 레인을 공유함

---

### 🔥 13. GPU 길이 ↔ 케이스 지원 길이
**검증 내용**: GPU 카드 길이가 케이스 최대 길이 이내인지 확인

**예시**:
- ✅ **정상**: GPU 320mm + 케이스 최대 350mm
- ❌ **오류**: GPU 360mm + 케이스 최대 320mm

**참고**: 
- 미들타워: 보통 300~350mm
- 풀타워: 보통 400mm 이상
- 미니케이스: 보통 250mm 이하

---

### 🔥 14. GPU 두께(슬롯) ↔ 케이스 지원
**검증 내용**: GPU 두께가 케이스 슬롯 공간에 맞는지 확인

**예시**:
- ✅ **정상**: GPU 2.5슬롯 + 케이스 최대 3슬롯
- ❌ **오류**: GPU 3.5슬롯 + 케이스 최대 3슬롯

**참고**: 
- 일반 GPU: 2~2.5슬롯
- 고성능 GPU: 3~3.5슬롯 (두꺼운 쿨러)

---

### 🔥 15. 전력 소비 ↔ PSU 용량
**검증 내용**: CPU + GPU 전력 소비가 PSU 용량의 70% 이내인지 확인

**계산 공식**: 권장 PSU = (CPU TDP + GPU TDP) × 1.5

**예시**:
- ✅ **정상**: CPU 125W + GPU 320W = 445W 예상 → PSU 750W (충분)
- ⚠️ **경고**: CPU 125W + GPU 320W = 445W 예상 → PSU 550W (부족)

**참고**: 
- RTX 4090: 450W (권장 PSU 1000W)
- RTX 4070: 200W (권장 PSU 650W)
- Ryzen 9: 170W
- Core i9: 150W

---

### 🔥 16. 파워 커넥터 ↔ GPU 요구사항
**검증 내용**: PSU가 GPU에 필요한 전원 커넥터를 제공하는지 확인

**예시**:
- ✅ **정상**: GPU 12VHPWR + PSU 12VHPWR 지원
- ❌ **오류**: GPU 12VHPWR + PSU 8pin만 지원 (어댑터 필요)

**커넥터 종류**:
- **8pin**: 구형 GPU (RTX 30/20 시리즈)
- **12VHPWR**: 최신 GPU (RTX 40 시리즈)

---

### 🔥 17. 파워 크기(SFX/ATX) ↔ 케이스
**검증 내용**: PSU 크기가 케이스에 장착 가능한지 확인

**예시**:
- ✅ **정상**: SFX PSU + 미니케이스 (SFX 지원)
- ❌ **오류**: ATX PSU + 미니케이스 (SFX만 지원)

**크기 종류**:
- **ATX**: 표준 크기 (140~160mm 깊이)
- **SFX**: 소형 (100mm 깊이)
- **SFX-L**: 중형 (125mm 깊이)

---

## 🚀 검증 프로세스

```javascript
// AI 호환성 검사 실행
const checkCompatibility = () => {
    const results = {
        success: [],    // ✅ 정상 호환
        warnings: [],   // ⚠️ 경고 (사용 가능하지만 주의 필요)
        errors: [],     // ❌ 오류 (호환 불가)
        suggestions: [] // 💡 AI 제안 (해결 방법)
    };

    // 1~17번 검증 실행
    // ...

    // 결과 표시
    showCompatibilityResults(results);
};
```

---

## 📊 결과 표시 형식

```
=== AI 호환성 검사 결과 ===

✅ 정상:
  • CPU (LGA1700) ↔ 쿨러 소켓 호환
  • 메인보드 ↔ RAM DDR5 호환
  • GPU 길이 (320mm) ↔ 케이스 지원 (350mm)
  • PSU 용량 (750W) 충분 (권장: 650W)

⚠️ 경고:
  • Ryzen 5000 시리즈는 B450 칩셋에서 BIOS 업데이트가 필요할 수 있습니다

❌ 오류:
  • GPU 두께 3.5슬롯이 케이스 지원 3슬롯을 초과합니다

💡 AI 제안:
  • 더 넓은 슬롯 공간을 제공하는 케이스를 선택하세요
  • PSU 커넥터에서 "12VHPWR"를 선택하세요
```

---

## 🎯 사용 시나리오

### 시나리오 1: 완벽한 호환성
```
사용자 선택:
- CPU: Intel 14세대 (LGA1700)
- 쿨러: 타워형 (LGA1700 지원, TDP 180W)
- 메인보드: Z790 (DDR5, 4x M.2, 6x SATA)
- RAM: DDR5 32GB (6000MHz)
- GPU: RTX 4070 (8GB, 200W, 280mm, 2.5슬롯, 8pin)
- PSU: 750W (80+ Gold, ATX, 8pin x3)
- 케이스: 미들타워 (ATX, 350mm GPU, 180mm 쿨러)

결과:
✅ 모든 호환성 검사를 통과했습니다! (17/17)
```

### 시나리오 2: 경고 있음 (사용 가능)
```
사용자 선택:
- CPU: Ryzen 5000
- 메인보드: B450

결과:
⚠️ Ryzen 5000 시리즈는 B450 칩셋에서 BIOS 업데이트가 필요할 수 있습니다
💡 메인보드 제조사 홈페이지에서 최신 BIOS를 다운로드하세요
```

### 시나리오 3: 오류 있음 (조합 불가)
```
사용자 선택:
- GPU: RTX 4090 (450W, 12VHPWR)
- PSU: 650W (8pin x2만 지원)

결과:
❌ PSU 용량 부족: 예상 600W → 권장 1000W (선택: 650W)
❌ GPU가 12VHPWR 커넥터를 요구하지만, PSU가 지원하지 않습니다
💡 PSU를 1000W 이상, 12VHPWR 지원 모델로 변경하세요
```

---

## 📚 기술적 구현

### 상태 관리 (State)
```javascript
// 신규 추가된 상태 (8개)
const [coolerTdp, setCoolerTdp] = useState([65, 250]);           // 쿨러 냉각 능력
const [ramMaxCapacity, setRamMaxCapacity] = useState(128);       // 메인보드 최대 RAM
const [maxRamSpeed, setMaxRamSpeed] = useState(7200);            // 메인보드 최대 RAM 속도
const [xmpSupport, setXmpSupport] = useState('any');             // XMP/EXPO 지원
const [m2SataConflict, setM2SataConflict] = useState('any');     // M.2/SATA 충돌
const [gpuSlots, setGpuSlots] = useState(2.5);                   // GPU 두께
const [gpuConnector, setGpuConnector] = useState('');            // GPU 전원 커넥터
const [psuFormFactor, setPsuFormFactor] = useState('');          // PSU 크기
const [psuConnectors, setPsuConnectors] = useState([]);          // PSU 커넥터 지원
const [casePsuSupport, setCasePsuSupport] = useState([]);        // 케이스 PSU 지원
const [caseGpuSlots, setCaseGpuSlots] = useState(4);             // 케이스 GPU 슬롯
```

### 검증 로직 예시 (BIOS 호환성)
```javascript
// 10. CPU 세대 ↔ 메인보드 BIOS 지원
if (cpuGeneration && chipset.length > 0) {
    let biosCompatible = false;
    if (cpuGeneration.includes('Intel 14세대') && 
        (chipset.includes('Z790') || chipset.includes('B760'))) {
        biosCompatible = true;
    } else if (cpuGeneration.includes('Ryzen 5000') && 
               (chipset.includes('X570') || chipset.includes('B550') || chipset.includes('B450'))) {
        biosCompatible = true;
        results.warnings.push('Ryzen 5000 시리즈는 B450 칩셋에서 BIOS 업데이트가 필요할 수 있습니다');
    }

    if (biosCompatible) {
        results.success.push(`CPU 세대 (${cpuGeneration}) ↔ 메인보드 칩셋 호환`);
    } else {
        results.errors.push(`CPU 세대 (${cpuGeneration})와 메인보드 칩셋 (${chipset.join(', ')})이 불일치합니다`);
        results.suggestions.push('CPU 세대에 맞는 메인보드 칩셋을 선택하세요');
    }
}
```

---

## 🔮 향후 개선 사항

1. **백엔드 통합**: 실제 부품 데이터베이스와 연동하여 정확한 호환성 검증
2. **실시간 가격 정보**: 각 부품의 실시간 시장 가격 반영
3. **모니터 포트 호환성**: DisplayPort/HDMI 버전 검증 추가
4. **운영체제 호환성**: Windows 11 TPM 요구사항 검증 추가
5. **케이스 에어플로우 분석**: 냉각 성능 시뮬레이션
6. **전력 효율 계산**: 80+ 등급별 실제 소비 전력 예측

---

## ✨ 핵심 설계 철학

> **"구멍사이즈가 안맞으면 장착부터안되니까"**

사용자의 이 한마디가 시스템의 핵심 철학입니다.  
물리적 호환성(소켓, 포트, 크기)뿐만 아니라, **실제 조립 가능성**까지 검증합니다.

- ✅ **논리적 호환성**: CPU 소켓 ↔ 메인보드 칩셋
- ✅ **물리적 호환성**: M.2 슬롯 수, SATA 포트 수, GPU 길이, 쿨러 높이
- ✅ **전력 호환성**: PSU 용량, GPU 커넥터
- ✅ **BIOS 호환성**: CPU 세대 ↔ 메인보드 펌웨어

**모든 검증이 통과되어야만 실제로 조립 가능한 PC**라고 판단합니다.

---

## 📞 문의

이 호환성 검사 시스템에 대한 문의나 개선 제안은 프로젝트 이슈 트래커를 이용해 주세요.

**문서 작성일**: 2025-11-14  
**버전**: 2.0 (17개 필수 체크 완전 구현)
